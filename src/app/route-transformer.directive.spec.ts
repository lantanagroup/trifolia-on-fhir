import { RouteTransformerDirective } from './route-transformer.directive';

describe('RouteTransformerDirective', () => {
  it('should create an instance', () => {
    const directive = new RouteTransformerDirective();
    expect(directive).toBeTruthy();
  });
});
